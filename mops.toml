[package]
name = "geecs"
version = "1.0.0"
description = "Ghost Engine ECS (GEECS)"
repository = "https://github.com/jneums/geecs"
keywords = [ "ecs", "game", "simulations", "ghost-engine" ]
license = "MIT"

[dependencies]
base = "0.13.0"
stable-hash-map = "0.0.1"
vector = "0.4.1"

[dev-dependencies]
test = "2.0.0"

[toolchain]
moc = "0.13.0"
